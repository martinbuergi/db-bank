export default async function decorate(block) {
  // The block is a simple centered CTA button.
  // EDS decorateButtons() handles the link → button transformation.
  // No additional DOM manipulation needed.
}
